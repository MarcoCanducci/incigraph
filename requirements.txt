# Runtime dependencies for the InciGraph Streamlit app.
#
# Streamlit Community Cloud reads this file to build the app environment.
# For local development, `pip install -e ".[plots]"` covers the same set.
#
# IMPORTANT: the final line installs the InciGraph package itself from the
# repository root (where pyproject.toml lives). Without it, `import incigraph`
# fails on Community Cloud, because Community Cloud only installs what is
# listed here -- it does NOT auto-install the repo's own package.
#
# The "." refers to the repo root. Community Cloud runs pip from there, so
# pip reads pyproject.toml and installs the incigraph package + its deps.

streamlit>=1.30
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
scipy>=1.10
matplotlib>=3.7
requests>=2.28

# Install the InciGraph package itself from the repo root:
.
